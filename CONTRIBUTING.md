# Contributing to J.H AI Agent

## ライセンスについて（読んでください）

### 貢献は inbound = outbound です

このリポジトリに送っていただいたコードは、**プロジェクト本体と同じ条件**
（`MIT OR Apache-2.0`）で提供されたものとして扱います。追加の書類は不要です。
プルリクエストを送る行為そのものが、その意思表示とみなされます。

これは Rust / Node エコシステムで最も一般的な形で、貢献者にとっては「読むだけ」、
メンテナにとっては「署名を集めなくていい」という点で、両者にとって摩擦が最小です。

### DCO — 由来の保証

コミットには **`Signed-off-by:` 行**を入れてください。

```bash
git commit -s -m "Fix the thing"
```

`-s` が自動で追加します:

```
Signed-off-by: Your Name <your.email@example.com>
```

これは **Developer Certificate of Origin (DCO) 1.1** への同意を意味します。
全文は <https://developercertificate.org/> にあります（短いので一度読んでください）。
要点は「あなたにはこのコードを提供する権利がある」という**あなた自身の表明**です:

- 自分で書いたコードである、または
- 適切なライセンスの下で入手し、その条件を守れる形で提供している、そして
- 貢献内容が公開記録として残ることを理解している

**勤務先の業務として書いたコードには注意してください。** その著作権は多くの場合
雇用主に帰属します。会社のコードを個人名義で提供する権限があるか、事前に確認して
ください。これは DCO が保証を求めている、まさにその点です。

### DCO と CLA の違い（よくある誤解）

| | DCO | CLA |
|---|---|---|
| 形式 | コミットに1行 | 署名（電子署名または書面） |
| 保証すること | 提供する**権利がある** | 上記 ＋ プロジェクトへの**広い許諾** |
| メンテナが将来できること | ライセンスの**変更はできない** | 再ライセンス・独自条件での再配布が可能 |
| 貢献者の手間 | ほぼゼロ | 署名手続きが必要 |

**現状 DCO を採用しています。** 理由は、`MIT OR Apache-2.0` で受け取った貢献は
MIT の "sublicense" 権により**すでに商用製品への組み込みが可能**であり、CLA が
追加で与えるのは「将来、プロジェクト全体を別ライセンス（BSL や AGPL など）へ
**変更する**権利」だけだからです。その予定がない限り、CLA の手続き負荷は
貢献者にとって純粋なコストになります。

言い換えると: **CLA がないと、将来ライセンスを変更したくなったときに全貢献者の
同意が必要になります。** 逆に、オープンコアや商用版の提供には CLA は不要です。
方針を変える場合は、外部からの貢献を受け入れ始める**前に**決めてください。
貢献者が増えてからでは、遡って全員から同意を取ることになります。

### 第三者のコードを持ち込む場合

他所からコードを引用・移植する場合は、**PR の説明に出典とライセンスを明記**して
ください。互換性の確認が必要です。

- **GPL / AGPL のコードは受け入れられません。** 配布物全体を同じ条件にする義務が
  生じ、`MIT OR Apache-2.0` での配布が不可能になります
- MIT / BSD / Apache-2.0 は歓迎ですが、**著作権表示を残す義務**があります。
  バイナリ（`.wasm` など）を追加する場合は `THIRD-PARTY-NOTICES.md` と、
  該当ディレクトリの `LICENSE` の両方を更新してください
- **著作権者を推測で書かないでください。** 上流の `LICENSE` を実際に取得して
  そのまま転記します。手順は `THIRD-PARTY-NOTICES.md §1` にあります
  （実例として、tree-sitter の4つの grammar は同じ GitHub organization に
  ありながら著作権者が3人異なります）

## 開発

```bash
npm install
npm run tauri dev
```

### 出す前に通すもの

```bash
npm test
```

```bash
npm run build
```

```bash
cd src-tauri && cargo test && cargo check
```

`cargo check` は**警告ゼロ**を維持してください。

### テストについて

新しい挙動にはテストを付けてください。とくに、**壊れても気づけない**種類の
ロジック（判定・文言・境界条件）は純粋関数として切り出し、表形式のテストを
書く方針です。既存の `src/modules/license/`, `src/i18n/`,
`src/modules/ai/agent/stopReason.js` が参考になります。

### コメントについて

「何をしているか」ではなく、**なぜそうなっているか**を書いてください。
とくに、一見おかしく見えるコードには理由を残してください。後から来た人が
「これは無駄だ」と判断して消すのを防ぐためです。
